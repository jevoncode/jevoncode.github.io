<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Tomcat优化以达到高吞吐快速失败系统</title>
    <link href="/2020/11/23/tomcat-you-hua-yi-da-dao-gao-tun-tu-kuai-su-shi-bai-xi-tong/"/>
    <url>/2020/11/23/tomcat-you-hua-yi-da-dao-gao-tun-tu-kuai-su-shi-bai-xi-tong/</url>
    
    <content type="html"><![CDATA[<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>奈飞有许多高吞吐低延迟的中间层服务（mid tier services，估计翻译中间件）。在这些服务中，他们发现有些服务，在在很短的时间剧增的流量，会导致这些机子变成CPU饥饿(cpu-starved)，并且服务会没响应。会导致这些服务的客户端体验很差，如读超时和连接超时。特别如果将读超时设置得非常高，会导致特别差的体验，客户端会等到好久好久。在SOA架构中，客户端的客户端也会请求超时，从而导致雪崩效应（ripple effect），以至于整个应用的其他服务也一起都变慢或不可用，最后全部服务变慢或不可用。在正常情况，机子都是有大量的CPU资源，服务也不是CPU密集型（cpu intensive）。所以，为什么会导致上述那些异常情况发发生？为了搞懂这些，熟悉我们要从最高层看看这些服务层次，请求流程如下：</p><pre><code class="hljs brainfuck">--&gt; <span class="hljs-comment">Apache</span> --&gt; <span class="hljs-comment">Tomcat</span></code></pre><p>在测试环境模拟流量激增时，发现CPU不足（cpu starvation）的原因是Apache和tomcat配置不当。当突然增加流量，Apache的worker就会变得繁忙和非常大量tomcat线程也在繁忙。系统CPU有个巨大的跳跃，是当大部分CPU时间都在处理上下文切换，就没有线程可以做任何有意义的工作时。</p><h1 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h1><p>由于这是个中间层服务，所以apache使用不多（apache压力不大）。所以与其优化两个系统（apache和tomcat），不如简单化这层次，直接关注tomcat优化。为了了解为什么tomcat的线程会繁忙，我们需要了解tomcat的线程模型。</p><h1 id="高层次的描述Tomcat-Http-Connectotr的线程模型"><a href="#高层次的描述Tomcat-Http-Connectotr的线程模型" class="headerlink" title="高层次的描述Tomcat Http Connectotr的线程模型"></a>高层次的描述Tomcat Http Connectotr的线程模型</h1><p>Tomcat有一个acceptor线程来接收连接（这里就涉及到对网络的熟悉，熟悉就知道连接不仅仅是socket的表面，更是tcp的三次握手过程）。另外还有线程池来做实际的工作。于是一个请求的过程是：</p><ol><li>OS和客户端建立连接的TCP握手。这取决于OS的实现，可能是有一个队列来保存这些连接或多个队列来保存。在多队列的情况，一条队列保存未完成的连接，这些连接都是还没完成三次握手的。一旦完成握手，连接就会被移到保存完成连接的队列，应用就会消费这队列里的连接。“acceptCount”这tomcat参数就用于控制这些队列的长度。（acceptCount是指完成握手的连接数，acceptCount会被映射成backlog）<pre><code class="hljs java"><span class="hljs-comment">//org.apache.catalina.connector.Connector</span><span class="hljs-keyword">protected</span> <span class="hljs-keyword">static</span> HashMap&lt;String,String&gt; replacements =            <span class="hljs-keyword">new</span> HashMap&lt;String,String&gt;();    <span class="hljs-keyword">static</span> &#123;        replacements.put(<span class="hljs-string">&quot;acceptCount&quot;</span>, <span class="hljs-string">&quot;backlog&quot;</span>);        replacements.put(<span class="hljs-string">&quot;connectionLinger&quot;</span>, <span class="hljs-string">&quot;soLinger&quot;</span>);        replacements.put(<span class="hljs-string">&quot;connectionTimeout&quot;</span>, <span class="hljs-string">&quot;soTimeout&quot;</span>);        replacements.put(<span class="hljs-string">&quot;rootFile&quot;</span>, <span class="hljs-string">&quot;rootfile&quot;</span>);    &#125;</code></pre>backlog在linux2.2就变为指定完成握手的队列长度，而未完成的握手的半连接队列长度则通过操作系统/proc/sys/net/ipv4/tcp_max_syn_backlog来设置.<blockquote><p>int listen(int sockfd, int backlog);<br>DESCRIPTION  listen() marks the socket referred to by sockfd as a passive socket, that is, as a socket that will be used to accept incoming connection requests using accept(2).<br>The backlog argument defines the maximum length to which the queue of pending connections for sockfd may grow. If a connection request arrives when the queue is full, the client may receive an error with an indication of  ECONNREFUSED or, if the underlying protocol supports retransmission, the request may be ignored so that a later reattempt at connection succeeds.<br>The behavior of the backlog argument on TCP sockets changed with Linux 2.2. Now it specifies the queue length for completely established sockets waiting to be accepted, instead of the number of incomplete connection        requests.  The maximum length of the queue for incomplete sockets can be set using /proc/sys/net/ipv4/tcp_max_syn_backlog. When syncookies are enabled there is no logical maximum length and this setting is ignored. See  tcp(7) for more information.</p></blockquote></li></ol><ol start="2"><li><p>tomcat的acceptor线程接收连接，这些队列都是来自于已完成握手的队列。</p></li><li><p>检查工作线程池是否有空闲的线程，如果没有且活动线程数小于maxThreads，则会创建工作线程，否则等待空闲线程。</p></li><li><p>一旦有空闲工作线程，acceptor线程就会将连接交给工作线程后，然后继续监听新的连接。</p></li><li><p>工作线程做的就是实际的工作，如从连接读取输入，处理请求，然后发送响应给客户端。如果连接不是keep alive则会关闭连接，然后将自己放回线程池。如果是keep aliave连接，继续等待该连接读取输入。如果数据一直没到，那么keep alive情况，会有个keepAliveTimeout，超过该时间，则会关闭连接，然后将自己放回线程池。</p></li></ol><p>考虑这种情况，tomcat的maxThreads和acceptCount设置都很大，突增的流量会填满OS的队列和让tomcat的所有线程都变得繁忙。当更多的请求发送到这台机子，从而超过系统所能处理的数量时，这种请求的“排队”是不可避免的，并会导致繁忙线程的增加，最终导致CPU不足（cpu starvation）。因此，解决方法的关键是避免多个点（OS和tomcat线程）上有太多排队的请求，并在应用程序达到最大容量时快速失败（返回http状态503）。以下是实际操作的一个推荐：</p><h1 id="当达到系统容量，应快速失败"><a href="#当达到系统容量，应快速失败" class="headerlink" title="当达到系统容量，应快速失败"></a>当达到系统容量，应快速失败</h1><p>预估在峰值负载时繁忙的线程数。如果服务器平均5ms内对请求作出响应，那么单个线程每秒则可处理200个请求(rps)。如果机子是4核CPU，则可以达到800rps。假设4个请求并行发送到机子（假设机子有4核），这会让4个线程繁忙5ms，所以下个5ms，4个或更多的请求让4个线程繁忙。随后的请求会选取一个空闲线程。所以理论上，在800rps时，平均不应该有超过8个线程处理繁忙状态。但实际当中，会有些不同，是因为系统所有资源都是共享的。因此，应该对系统能够维持的总吞吐量进行实验，并计算繁忙线程的期望数量。这将为维持峰值负载所需的线程数量提供一个基线。为了提供一些缓冲区，需要将线程数增加三倍以上，达到30个。这个缓冲区是任意的，如果需要还可以进一步调优。在我们的实验中，我们使用了略多于3倍的缓冲区，效果很好。<br>跟踪内存中运行中并发请求的数量，并将其用于快速失败。例如，当并发请求的数量接近刚刚预估的繁忙线程数据（8个），则返回一个http状态码503。这将防止太多的工作线程变得繁忙，因为一旦达到峰值吞吐了，任何变得活跃的额外线程都将执行非常轻量级的工作，即返回503。</p><h1 id="设置操作系统参数"><a href="#设置操作系统参数" class="headerlink" title="设置操作系统参数"></a>设置操作系统参数</h1><p>acceptCount参数用于tomcat表示最长队列，这队列是操作系统级别，用于处理还未完成tcp握手的操作（具体取决于OS）。这是一个很重要的调优参数，否则在建立连接时会出现连接不上，活着导致OS队列中的连接过度排队，从而导致读超时。当然每个OS处理正在握手和完成握手的连接细节是不同，可能会时只有一个连接队列，或多个连接队列用于区分存放未完成握手和完成握手的连接（请阅读相关的文档来获取这些细节）。所以，有一个很好的方法调优这个acceptCount参数，那就是从很小的值开始测试，逐步增大，增达到没有连接错误即可。<br>太大的acceptCount值意味OS层面可以接收更多的请求，但是，如果rps大于该机子能够处理的能力，所有工作线程会变得繁忙，于是aceeptor线程会等待，直至有worker线程空闲。更多的请求将继续堆积在OS队列中，因为只有当工作线程可用时，acceptor线程才可以使用它们。在最糟糕的情况，这些请求还在OS队列时就已经超时了，但是tomcat的acceptor线程依然会去获取它来交给工作线程处理。这种是完全浪费资源，而客户端也没收到任何响应。<br>如果acceptCount设置太小，则会在很高的rps时无法有足够的OS空间来接收连接，这样客户端就会手连接超时的错误（connect time out error），实际吞吐会低于服务器能够承载的。</p><p>因此可以实验从很小的值如10开始，然后逐步增加acceptCount的值，直至没有连接错误出现。</p><p>当完成上面两个改变后，就算最差的情况，所有工作线程都很繁忙，但机子不会cpu不足，依然有能力做更多的工作（最大吞吐）。</p><h1 id="其他考虑"><a href="#其他考虑" class="headerlink" title="其他考虑"></a>其他考虑</h1><p>如上所述，每个连接最终都被tomcat的一个工作线程处理。假如keep alive被打开，工作线程就会继续监听该连接，从而不会变成空闲而返回到线程池中。所以，如果客户端不够智能从而不会关闭连接，那么服务端很快就会用完它的线程。如果keep alive被打开，那么必须通过记住这种情况，来调节服务器群的大小。<br>或者，如果keep alive是关闭的，那么就不必担心使用工作线程处理非活动连接的问题。但是，在这种情况，每个调用就要付出打开和关闭连接的代价。此外，这还会创建很多TIME_WAIT状态的socket，这样会给服务器造成压力。</p><p>最好根据应用程序的用力进行选择，并通过运行实验来测试性能。</p><p>备注：<br>“连接”这两个字做于名词时，实际指的是socket。而当用于动词时，其实就是TCP三次握手。<br>线程池里的线程在本文，有时称为空闲线程或工作线程，或空闲工作线程。</p><p>参考：<br><a href="https://netflixtechblog.com/tuning-tomcat-for-a-high-throughput-fail-fast-system-e4d7b2fc163f">https://netflixtechblog.com/tuning-tomcat-for-a-high-throughput-fail-fast-system-e4d7b2fc163f</a><br><a href="https://zhuanlan.zhihu.com/p/92924737">https://zhuanlan.zhihu.com/p/92924737</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Java里的锁</title>
    <link href="/2020/10/28/java-li-de-sou/"/>
    <url>/2020/10/28/java-li-de-sou/</url>
    
    <content type="html"><![CDATA[<p>源码：jdk8</p><h2 id="线程要不要锁住资源的角度，分为：悲观锁和乐观锁"><a href="#线程要不要锁住资源的角度，分为：悲观锁和乐观锁" class="headerlink" title="线程要不要锁住资源的角度，分为：悲观锁和乐观锁"></a>线程要不要锁住资源的角度，分为：悲观锁和乐观锁</h2><h3 id="悲观锁"><a href="#悲观锁" class="headerlink" title="悲观锁"></a>悲观锁</h3><p>概念：对于同一个数据的并发操作，悲观锁认为自己在使用该数据的时候别的线程来修改该数据的概率比较高，因此在获取数据的时候会先加锁，确保数据不会被别的线程修改。<br>实现：Java中的，synchronized关键字和Lock的实现类都是悲观锁。</p><h3 id="乐观锁"><a href="#乐观锁" class="headerlink" title="乐观锁"></a>乐观锁</h3><p>概念：对于同一个数据的并发操作，乐观锁认为自己在使用该数据的时候别的线程来修改该数据的概率比较低，因此不加锁，只在更新的时候判断有没别的线程修改过该数据即可。如果该数据没有被修改，则当前线程就可以修改数据成功，否则数据已被其他线程修改过了，则返回修改失败或重试。<br>实现：Java中是通过使用无锁编程来实现，最常采用的是CAS算法。Java的原子类中的递增操作就是通过CAS实现的。</p><h3 id="乐观锁-VS-悲观锁"><a href="#乐观锁-VS-悲观锁" class="headerlink" title="乐观锁 VS 悲观锁"></a>乐观锁 VS 悲观锁</h3><p>在读操作和写操作是互斥的情况下：<br>悲观锁适合写操作多的场景。<br>乐观锁适合读操作多的场景。</p><h3 id="CAS算法"><a href="#CAS算法" class="headerlink" title="CAS算法"></a>CAS算法</h3><p>CAS全称Compare And Swap（比较与交换），是一种无锁算法。<br>CAS算法里涉及到三个操作数：</p><ul><li>需要读写的内存地址值V</li><li>进行比较的值A</li><li>需要写入的新值B</li></ul><p>算法：当且仅当V内存里的值等于A时，新值B替换V内存里的值，否则不执行任何操作。<br>这算法是一个原子操作，CPU指令集支持的。</p><p>AtomicInteger的源码如下：</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AtomicInteger</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Number</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">java</span>.<span class="hljs-title">io</span>.<span class="hljs-title">Serializable</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = <span class="hljs-number">6214790243416807050L</span>;    <span class="hljs-comment">// setup to use Unsafe.compareAndSwapInt for updates</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> Unsafe unsafe = Unsafe.getUnsafe();    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> valueOffset;    <span class="hljs-keyword">static</span> &#123;        <span class="hljs-keyword">try</span> &#123;            valueOffset = unsafe.objectFieldOffset                (AtomicInteger.class.getDeclaredField(<span class="hljs-string">&quot;value&quot;</span>));        &#125; <span class="hljs-keyword">catch</span> (Exception ex) &#123; <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(ex); &#125;    &#125;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">volatile</span> <span class="hljs-keyword">int</span> value;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * Atomically increments by one the current value.</span><span class="hljs-comment">     *</span><span class="hljs-comment">     * <span class="hljs-doctag">@return</span> the updated value</span><span class="hljs-comment">     */</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> <span class="hljs-title">incrementAndGet</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> unsafe.getAndAddInt(<span class="hljs-keyword">this</span>, valueOffset, <span class="hljs-number">1</span>) + <span class="hljs-number">1</span>;    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Unsafe</span></span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> <span class="hljs-title">getAndAddInt</span><span class="hljs-params">(Object var1, <span class="hljs-keyword">long</span> var2, <span class="hljs-keyword">int</span> var4)</span> </span>&#123;        <span class="hljs-keyword">int</span> var5;        <span class="hljs-keyword">do</span> &#123;            var5 = <span class="hljs-keyword">this</span>.getIntVolatile(var1, var2);        &#125; <span class="hljs-keyword">while</span>(!<span class="hljs-keyword">this</span>.compareAndSwapInt(var1, var2, var5, var5 + var4));        <span class="hljs-keyword">return</span> var5;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">native</span> <span class="hljs-keyword">int</span> <span class="hljs-title">getIntVolatile</span><span class="hljs-params">(Object var1, <span class="hljs-keyword">long</span> var2)</span></span>;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">native</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">compareAndSwapInt</span><span class="hljs-params">(Object var1, <span class="hljs-keyword">long</span> var2, <span class="hljs-keyword">int</span> var4, <span class="hljs-keyword">int</span> var5)</span></span>;&#125;</code></pre><p>CAS操作封装在compareAndSwapInt()中，在JNI里是借助于一个CPU指令完成的，属于原子操作，可以保证多个线程都能够看到同一个变量的修改值。<br>后续JDK通过CPU的cmpxchg指令，去比较寄存器中的A和V内存里的值。如果相等，就把要写入的新值 B 存入V内存中。如果不相等，就将V内存里的值赋值给寄存器中的值A。然后通过Java代码中的while循环再次调用cmpxchg指令进行重试，直到设置成功为止。</p><p>CAS虽然很高效，但是它也存在三大问题：</p><ol><li><p>ABA问题；<br>CAS需要在操作值的时候检查内存值是否发生变化，没有发生变化才会更新内存值。但是如果内存值原来是A，后来变成了B，然后又变成了A，那么CAS进行检查时会发现值没有发生变化，但是实际上是有变化的。ABA问题的解决思路就是在变量前面添加版本号，每次变量更新的时候都把版本号加一，这样变化过程就从“A－B－A”变成了“1A－2B－3A”。</p><blockquote><p>JDK从1.5开始提供了AtomicStampedReference类来解决ABA问题，具体操作封装在compareAndSet()中。compareAndSet()首先检查当前引用和当前标志与预期引用和预期标志是否相等，如果都相等，则以原子方式将引用值和标志的值设置为给定的更新值。</p></blockquote></li><li><p>循环时间长开销大；<br>CAS操作如果长时间不成功，会导致其一直自旋，给CPU带来非常大的开销。</p></li><li><p>只能保证一个共享变量的原子操作。<br>对一个共享变量执行操作时，CAS能够保证原子操作，但是对多个共享变量操作时，CAS是无法保证操作的原子性的。</p><blockquote><p>Java从1.5开始JDK提供了AtomicReference类来保证引用对象之间的原子性，可以把多个变量放在一个对象里来进行CAS操作。</p></blockquote></li></ol><h2 id="锁住同步资源失败，线程要不要阻塞的角度，分为：悲观锁和乐观锁"><a href="#锁住同步资源失败，线程要不要阻塞的角度，分为：悲观锁和乐观锁" class="headerlink" title="锁住同步资源失败，线程要不要阻塞的角度，分为：悲观锁和乐观锁"></a>锁住同步资源失败，线程要不要阻塞的角度，分为：悲观锁和乐观锁</h2><h3 id="自旋锁"><a href="#自旋锁" class="headerlink" title="自旋锁"></a>自旋锁</h3><p>由于阻塞或唤醒一个Java线程需要操作系统切换CPU上下文，这种切换是需要耗费处理器时间。所以我们可以让线程“稍等一下”，让当前线程自旋，如果在自旋完成后前面锁定同步资源的线程已经释放了锁，那么当前线程就可以不必阻塞而是直接获取同步资源，从而避免切换线程的开销。这锁定同步资源的过程就是自旋锁。</p><h3 id="适应性自旋锁"><a href="#适应性自旋锁" class="headerlink" title="适应性自旋锁"></a>适应性自旋锁</h3><p>自旋锁本身是有缺点的，它不能代替阻塞。自旋等待虽然避免了线程切换的开销，但它要占用处理器时间。如果锁被占用的时间很短，自旋等待的效果就会非常好。反之，如果锁被占用的时间很长，那么自旋的线程只会白浪费处理器资源。所以，自旋等待的时间必须要有一定的限度，如果自旋超过了限定次数（默认是10次，可以使用-XX:PreBlockSpin来更改）没有成功获得锁，就应当挂起线程。<br>自旋锁在JDK1.4.2中引入，使用-XX:+UseSpinning来开启。JDK 6中变为默认开启，并且引入了自适应的自旋锁（适应性自旋锁）。</p><p>自适应意味着自旋的时间（次数）不再固定，而是由前一次在同一个锁上的自旋时间及锁的拥有者的状态来决定。如果在同一个锁对象上，自旋等待刚刚成功获得过锁，并且持有锁的线程正在运行中，那么虚拟机就会认为这次自旋也是很有可能再次成功，进而它将允许自旋等待持续相对更长的时间。如果对于某个锁，自旋很少成功获得过，那在以后尝试获取这个锁时将可能省略掉自旋过程，直接阻塞线程，避免浪费处理器资源。</p><p>在自旋锁中 另有三种常见的锁形式:TicketLock、CLHlock和MCSlock，本文中仅做名词介绍，不做深入讲解，感兴趣的同学可以自行查阅相关资料。</p><h2 id="synchronized角度，分为：无锁、-偏向锁、-轻量级锁、-重量级锁"><a href="#synchronized角度，分为：无锁、-偏向锁、-轻量级锁、-重量级锁" class="headerlink" title="synchronized角度，分为：无锁、 偏向锁、 轻量级锁、 重量级锁"></a>synchronized角度，分为：无锁、 偏向锁、 轻量级锁、 重量级锁</h2><p>synchronized在JDK6之前，当前线程如果获取同步资源的锁失败，立刻让出CPU资源，也就是需要操作系统切换CPU上下文，这种切换是需要耗费处理器时间。这就是JDK6之前synchronized效率低的原因。这种依赖于操作系统Mutex Lock所实现的锁我们称之为“重量级锁”，JDK6中为了减少获得锁和释放锁带来的性能消耗，引入了“偏向锁”和“轻量级锁”。</p><p>目前synchronized锁一共有4种状态，级别从低到高依次是：无锁、偏向锁、轻量级锁和重量级锁。锁状态只能升级不能降级。</p><p>Synchroneized底层有两个重要概念：“Java对象头”和“Monitor”。</p><h3 id="Java对象头"><a href="#Java对象头" class="headerlink" title="Java对象头"></a>Java对象头</h3><p>Hotspot虚拟机里，对象头主要包括两部分数据：<strong>Mark Word</strong>和<strong>Klass Pointer</strong>。<br><strong>Mark Word</strong>: 默认存储对象的HashCode、分代年龄和锁标志位信息。</p><p><strong>Klass Point</strong>: 该对象指向它的类元数据的指针，虚拟机通过这个指针确定这个对象是哪个类的实例。</p><h3 id="Monitor"><a href="#Monitor" class="headerlink" title="Monitor"></a>Monitor</h3><p>Monitor可以理解微一个同步工具或一种同步机制，通常被描述为一个对象。<br>Monitor是线程私有的数据结构，每个线程都有一个可用monitor record列表，同时还有一个全局的可用列表。每一个被锁住的对象都会与一个Monitor关联，同时Monitor中又一个Owner字段存放拥有该锁的线程的唯一表示，表示该锁被这个线程占用。</p><p>synchronized通过Monitor来实现线程同步，Monitor是依赖于底层的操作系统的Mutex Lock（互斥锁）来实现的线程同步。</p><h3 id="结合-Java对象头和Monitor，synchronized就可以实现以下锁状态"><a href="#结合-Java对象头和Monitor，synchronized就可以实现以下锁状态" class="headerlink" title="结合 Java对象头和Monitor，synchronized就可以实现以下锁状态:"></a>结合 Java对象头和Monitor，synchronized就可以实现以下锁状态:</h3><p>|锁状态|存储内容｜存储内容|备注|<br>|—–|——-|——-|—|<br>|无锁|对象的hashCode、对象分代年龄、是否是偏向锁(0)|01|无|<br>|偏向锁|偏向线程ID、偏向时间戳、对象分代年龄、是否是偏向锁(1)|01|比无锁多了偏向锁状态(1)和偏向线程ID，偏向线程ID应该存储对象头的Mark Word里|<br>|轻量级锁|指向栈中锁记录的指针|00|站在另外个线程角度，以CAS形式去获取已经被其他线程获取的偏向锁|<br>|重量级锁|指向互斥量(重量级锁)的指针|10|保存在monitor里的锁数据|</p><h4 id="无锁"><a href="#无锁" class="headerlink" title="无锁"></a>无锁</h4><p>这是在synchronized的情况下:<br>无锁没有对资源进行锁定，所有的线程都能访问并修改同一个资源，但同时只有一个线程能修改成功。<br>就是乐观锁。</p><p>我猜估计在Mark Word有个cas的标示位可以做到CAS的修改对象</p><h4 id="偏向锁"><a href="#偏向锁" class="headerlink" title="偏向锁"></a>偏向锁</h4><p>偏向锁是指一段同步代码一直被一个线程所访问，那么该线程会自动获取锁，降低获取锁的代价。</p><p>在大多数情况下，锁总是由同一线程多次获得，不存在多线程竞争，所以出现了偏向锁。其目标就是在只有一个线程执行同步代码块时能够提高性能。</p><p>当一个线程访问同步代码块并获取锁时，会在Mark Word里存储锁偏向的线程ID。在线程进入和退出同步块时不再通过CAS操作来加锁和解锁，而是检测Mark Word里是否存储着指向当前线程的偏向锁。引入偏向锁是为了在无多线程竞争的情况下尽量减少不必要的轻量级锁执行路径，因为轻量级锁的获取及释放依赖多次CAS原子指令，而偏向锁只需要在置换ThreadID的时候依赖一次CAS原子指令即可。</p><p>偏向锁只有遇到其他线程尝试竞争偏向锁时，持有偏向锁的线程才会释放锁，线程不会主动释放偏向锁。偏向锁的撤销，需要等待全局安全点（在这个时间点上没有字节码正在执行），它会首先暂停拥有偏向锁的线程，判断锁对象是否处于被锁定状态。撤销偏向锁后恢复到无锁（标志位为“01”）或轻量级锁（标志位为“00”）的状态。</p><p>偏向锁在JDK 6及以后的JVM里是默认启用的。可以通过JVM参数关闭偏向锁：-XX:-UseBiasedLocking=false，关闭之后程序默认会进入轻量级锁状态</p><h4 id="轻量级锁"><a href="#轻量级锁" class="headerlink" title="轻量级锁"></a>轻量级锁</h4><p>是指当锁是偏向锁的时候，被另外的线程所访问，偏向锁就会升级为轻量级锁，其他线程会通过自旋的形式尝试获取锁，不会阻塞，从而提高性能。</p><p>在代码进入同步块的时候，如果同步对象锁状态为无锁状态（锁标志位为“01”状态，是否为偏向锁为“0”），虚拟机首先将在当前线程的栈帧中建立一个名为锁记录（Lock Record）的空间，用于存储锁对象目前的Mark Word的拷贝，然后拷贝对象头中的Mark Word复制到锁记录中。</p><p>拷贝成功后，虚拟机将使用CAS操作尝试将对象的Mark Word更新为指向Lock Record的指针，并将Lock Record里的owner指针指向对象的Mark Word。</p><p>如果这个更新动作成功了，那么这个线程就拥有了该对象的锁，并且对象Mark Word的锁标志位设置为“00”，表示此对象处于轻量级锁定状态。</p><p>如果轻量级锁的更新操作失败了，虚拟机首先会检查对象的Mark Word是否指向当前线程的栈帧，如果是就说明当前线程已经拥有了这个对象的锁，那就可以直接进入同步块继续执行，否则说明多个线程竞争锁。</p><p>若当前只有一个等待线程，则该线程通过自旋进行等待。但是当自旋超过一定的次数，或者一个线程在持有锁，一个在自旋，又有第三个来访时，轻量级锁升级为重量级锁。</p><h4 id="重量级锁"><a href="#重量级锁" class="headerlink" title="重量级锁"></a>重量级锁</h4><p>升级为重量级锁时，锁标志的状态值变为“10”，此时Mark Word中存储的是指向重量级锁的指针，此时等待锁的线程都会进入阻塞状态。<br>综上，偏向锁通过对比Mark Word解决加锁问题，避免执行CAS操作。而轻量级锁是通过用CAS操作和自旋来解决加锁问题，避免线程阻塞和唤醒而影响性能。重量级锁是将除了拥有锁的线程以外的线程都阻塞。</p><h2 id="多个线程竞争锁时要不要排队的角度，分为：公平锁-排队-和非公平锁-先尝试插队，插队失败在排队"><a href="#多个线程竞争锁时要不要排队的角度，分为：公平锁-排队-和非公平锁-先尝试插队，插队失败在排队" class="headerlink" title="多个线程竞争锁时要不要排队的角度，分为：公平锁(排队)和非公平锁(先尝试插队，插队失败在排队)"></a>多个线程竞争锁时要不要排队的角度，分为：公平锁(排队)和非公平锁(先尝试插队，插队失败在排队)</h2><h3 id="公平锁"><a href="#公平锁" class="headerlink" title="公平锁"></a>公平锁</h3><p>公平锁是指多个线程按照申请锁的顺序来获取锁，线程直接进入队列中排队，队列中的第一个线程才能获得锁。<br>优点：<br>    等待锁的线程不会饿死。<br>缺点：<br>    整体吞吐效率相对非公平锁要低，等待队列中除第一个线程以外的所有线程都会阻塞，CPU唤醒阻塞线程的开销比非公平锁大，所以非公平锁插队减少CPU上下文的切换。</p><h3 id="非公平锁"><a href="#非公平锁" class="headerlink" title="非公平锁"></a>非公平锁</h3><p>非公平锁是多个线程加锁时直接尝试获取锁，获取不到才会到等待队列的队尾等待。但如果此时锁刚好可用，那么这个线程可以无需阻塞直接获取到锁，所以非公平锁有可能出现后申请锁的线程先获取锁的场景。<br>优点：<br>    减少唤起线程的开销，整体的吞吐效率高，因为线程有几率不阻塞直接获得锁，CPU不必唤醒所有线程。<br>缺点：<br>    处于等待队列中的线程可能会饿死，或者等很久才会获得锁。</p><p>通过ReentrantLock的源码来讲解下上述的锁</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ReentrantLock</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Lock</span>, <span class="hljs-title">java</span>.<span class="hljs-title">io</span>.<span class="hljs-title">Serializable</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = <span class="hljs-number">7373984872572414699L</span>;    <span class="hljs-comment">/** Synchronizer providing all implementation mechanics */</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Sync sync;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * Base of synchronization control for this lock. Subclassed</span><span class="hljs-comment">     * into fair and nonfair versions below. Uses AQS state to</span><span class="hljs-comment">     * represent the number of holds on the lock.</span><span class="hljs-comment">     */</span>    <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Sync</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractQueuedSynchronizer</span> </span>&#123;        ...    &#125;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * Sync object for non-fair locks</span><span class="hljs-comment">     */</span>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">NonfairSync</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Sync</span> </span>&#123;        ...    &#125;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * Sync object for fair locks</span><span class="hljs-comment">     */</span>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FairSync</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Sync</span> </span>&#123;        ...    &#125;     <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ReentrantLock</span><span class="hljs-params">()</span> </span>&#123;        sync = <span class="hljs-keyword">new</span> NonfairSync();    &#125;     <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ReentrantLock</span><span class="hljs-params">(<span class="hljs-keyword">boolean</span> fair)</span> </span>&#123;        sync = fair ? <span class="hljs-keyword">new</span> FairSync() : <span class="hljs-keyword">new</span> NonfairSync();    &#125;    ...&#125;</code></pre><p>ReentrantLock里面有一个内部类Sync，Sync继承AQS（AbstractQueuedSynchronizer）。AQS是定义锁的资源（state）和带队列功能的锁逻辑。 Sync实现的是state的CAS锁（也是偏向锁的逻辑），是非公平锁的逻辑，以及释放锁。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">nonfairTryAcquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> acquires)</span> </span>&#123;            <span class="hljs-keyword">final</span> Thread current = Thread.currentThread();            <span class="hljs-keyword">int</span> c = getState();            <span class="hljs-keyword">if</span> (c == <span class="hljs-number">0</span>) &#123;                <span class="hljs-keyword">if</span> (compareAndSetState(<span class="hljs-number">0</span>, acquires)) &#123;                    setExclusiveOwnerThread(current);                    <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;                &#125;            &#125;            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (current == getExclusiveOwnerThread()) &#123; 如果是当前线程，则获取锁成功                <span class="hljs-keyword">int</span> nextc = c + acquires;                <span class="hljs-keyword">if</span> (nextc &lt; <span class="hljs-number">0</span>) <span class="hljs-comment">// overflow</span>                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(<span class="hljs-string">&quot;Maximum lock count exceeded&quot;</span>);                setState(nextc);                <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;            &#125;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;        &#125;        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryRelease</span><span class="hljs-params">(<span class="hljs-keyword">int</span> releases)</span> </span>&#123;            <span class="hljs-keyword">int</span> c = getState() - releases;            <span class="hljs-keyword">if</span> (Thread.currentThread() != getExclusiveOwnerThread())                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalMonitorStateException();            <span class="hljs-keyword">boolean</span> free = <span class="hljs-keyword">false</span>;            <span class="hljs-keyword">if</span> (c == <span class="hljs-number">0</span>) &#123;                free = <span class="hljs-keyword">true</span>;                setExclusiveOwnerThread(<span class="hljs-keyword">null</span>);            &#125;            setState(c);            <span class="hljs-keyword">return</span> free;&#125;</code></pre><p>但ReentrantLock调用lock方法的，实际还是交给公平锁FairSync和非公平锁NonfairSync两个子类。ReentrantLock默认使用非公平锁，也可以通过构造器来显示的指定使用公平锁。</p><p>而lock方法除了偏向锁之外逻辑，就是AQS的acquire方法，acquire方法就调用子类的tryAcquire方法，看一下公平锁与非公平锁的tryAcquire方法的源码:<br>公平锁的</p><pre><code class="hljs java"><span class="hljs-comment">//java.util.concurrent.locks.ReentrantLock.FairSync#tryAcquire</span> <span class="hljs-comment">/**</span><span class="hljs-comment">    * Fair version of tryAcquire.  Don&#x27;t grant access unless</span><span class="hljs-comment">    * recursive call or no waiters or is first.</span><span class="hljs-comment">    */</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryAcquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> acquires)</span> </span>&#123;    <span class="hljs-keyword">final</span> Thread current = Thread.currentThread();    <span class="hljs-keyword">int</span> c = getState();    <span class="hljs-keyword">if</span> (c == <span class="hljs-number">0</span>) &#123;        <span class="hljs-keyword">if</span> (!hasQueuedPredecessors() &amp;&amp;            compareAndSetState(<span class="hljs-number">0</span>, acquires)) &#123;            setExclusiveOwnerThread(current);            <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        &#125;    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (current == getExclusiveOwnerThread()) &#123;        <span class="hljs-keyword">int</span> nextc = c + acquires;        <span class="hljs-keyword">if</span> (nextc &lt; <span class="hljs-number">0</span>)            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(<span class="hljs-string">&quot;Maximum lock count exceeded&quot;</span>);        setState(nextc);        <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;&#125;<span class="hljs-comment">//java.util.concurrent.locks.AbstractQueuedSynchronizer#hasQueuedPredecessors</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">hasQueuedPredecessors</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-comment">// The correctness of this depends on head being initialized</span>    <span class="hljs-comment">// before tail and on head.next being accurate if the current</span>    <span class="hljs-comment">// thread is first in queue.</span>    Node t = tail; <span class="hljs-comment">// Read fields in reverse initialization order</span>    Node h = head;    Node s;    <span class="hljs-keyword">return</span> h != t &amp;&amp;        ((s = h.next) == <span class="hljs-keyword">null</span> || s.thread != Thread.currentThread());        &#125;</code></pre><pre><code class="hljs java">涉及到离散数学，先处理下!(h != t &amp;&amp;((s = h.next) == <span class="hljs-keyword">null</span> || s.thread != Thread.currentThread()))h=t||!((s = h.next) == <span class="hljs-keyword">null</span> || s.thread != Thread.currentThread())h=t||((s = h.next) != <span class="hljs-keyword">null</span>)&amp;&amp;s.thread == Thread.currentThread())   <span class="hljs-comment">//头尾相等，那就是队列为空，或head就是当前线程</span></code></pre><p>hasQueuedPredecessors这个逻辑判断有点复杂，结合!hasQueuedPredecessors()，离散处理下，那就是头尾相等，那就是队列为空，或head就是当前线程，那就可以cas去获取锁。<br>直白的说，必须是队列的头部才能获取锁，否则得进入排队。</p><p>非公平锁的</p><pre><code class="hljs java"><span class="hljs-comment">// java.util.concurrent.locks.ReentrantLock.NonfairSync#tryAcquire</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryAcquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> acquires)</span> </span>&#123;            <span class="hljs-keyword">return</span> nonfairTryAcquire(acquires);&#125;<span class="hljs-comment">//java.util.concurrent.locks.ReentrantLock.Sync#nonfairTryAcquire</span><span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">nonfairTryAcquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> acquires)</span> </span>&#123;            <span class="hljs-keyword">final</span> Thread current = Thread.currentThread();            <span class="hljs-keyword">int</span> c = getState();            <span class="hljs-keyword">if</span> (c == <span class="hljs-number">0</span>) &#123;                <span class="hljs-keyword">if</span> (compareAndSetState(<span class="hljs-number">0</span>, acquires)) &#123;                    setExclusiveOwnerThread(current);                    <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;                &#125;            &#125;            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (current == getExclusiveOwnerThread()) &#123;                <span class="hljs-keyword">int</span> nextc = c + acquires;                <span class="hljs-keyword">if</span> (nextc &lt; <span class="hljs-number">0</span>) <span class="hljs-comment">// overflow</span>                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(<span class="hljs-string">&quot;Maximum lock count exceeded&quot;</span>);                setState(nextc);                <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;            &#125;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;&#125;</code></pre><p>上述公平锁和非公平锁都没见到哪里进去排队了，判断获取锁失败就进入队列的逻辑是在AQS的acquire方法</p><pre><code class="hljs java"><span class="hljs-comment">// java.util.concurrent.locks.AbstractQueuedSynchronizer#acquire</span>  <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title">acquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> arg)</span> </span>&#123;        <span class="hljs-keyword">if</span> (!tryAcquire(arg) &amp;&amp;            acquireQueued(addWaiter(Node.EXCLUSIVE), arg))            selfInterrupt();    &#125;</code></pre><p>其中addWaiter就根据当前线程创建node并加入队列里，设置当前模式为排他锁模式。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> Node <span class="hljs-title">addWaiter</span><span class="hljs-params">(Node mode)</span> </span>&#123;       Node node = <span class="hljs-keyword">new</span> Node(Thread.currentThread(), mode);       <span class="hljs-comment">// Try the fast path of enq; backup to full enq on failure</span>       Node pred = tail;       <span class="hljs-keyword">if</span> (pred != <span class="hljs-keyword">null</span>) &#123;           node.prev = pred;           <span class="hljs-keyword">if</span> (compareAndSetTail(pred, node)) &#123;               pred.next = node;               <span class="hljs-keyword">return</span> node;           &#125;       &#125;       enq(node);       <span class="hljs-keyword">return</span> node;   &#125;</code></pre><p>而acquireQueued就是调用</p><pre><code class="hljs java"><span class="hljs-function">inal <span class="hljs-keyword">boolean</span> <span class="hljs-title">acquireQueued</span><span class="hljs-params">(<span class="hljs-keyword">final</span> Node node, <span class="hljs-keyword">int</span> arg)</span> </span>&#123;        <span class="hljs-keyword">boolean</span> failed = <span class="hljs-keyword">true</span>;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-keyword">boolean</span> interrupted = <span class="hljs-keyword">false</span>;            <span class="hljs-keyword">for</span> (;;) &#123;                <span class="hljs-keyword">final</span> Node p = node.predecessor();                <span class="hljs-keyword">if</span> (p == head &amp;&amp; tryAcquire(arg)) &#123;         <span class="hljs-comment">//如果当前线程对应的节点的前驱是头部且当前线程获取到锁，则将头部的那个节点释放</span>                    setHead(node);                    p.next = <span class="hljs-keyword">null</span>; <span class="hljs-comment">// help GC</span>                    failed = <span class="hljs-keyword">false</span>;                    <span class="hljs-keyword">return</span> interrupted;                &#125;                <span class="hljs-keyword">if</span> (shouldParkAfterFailedAcquire(p, node) &amp;&amp;                    parkAndCheckInterrupt())                    interrupted = <span class="hljs-keyword">true</span>;            &#125;        &#125; <span class="hljs-keyword">finally</span> &#123;            <span class="hljs-keyword">if</span> (failed)                cancelAcquire(node);        &#125;    &#125;</code></pre><p>shouldParkAfterFailedAcquire的逻辑，将如果前驱也是在等待，那么当前节点也等待并且加入其尾部，如果前驱是cancel的，那么就继续往前找等待状态的前驱，加入到其尾部:</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">shouldParkAfterFailedAcquire</span><span class="hljs-params">(Node pred, Node node)</span> </span>&#123;        <span class="hljs-keyword">int</span> ws = pred.waitStatus;        <span class="hljs-keyword">if</span> (ws == Node.SIGNAL)            <span class="hljs-comment">/*</span><span class="hljs-comment">             * This node has already set status asking a release</span><span class="hljs-comment">             * to signal it, so it can safely park.</span><span class="hljs-comment">             */</span>            <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        <span class="hljs-keyword">if</span> (ws &gt; <span class="hljs-number">0</span>) &#123;            <span class="hljs-comment">/*</span><span class="hljs-comment">             * Predecessor was cancelled. Skip over predecessors and</span><span class="hljs-comment">             * indicate retry.</span><span class="hljs-comment">             */</span>            <span class="hljs-keyword">do</span> &#123;                node.prev = pred = pred.prev;            &#125; <span class="hljs-keyword">while</span> (pred.waitStatus &gt; <span class="hljs-number">0</span>);            pred.next = node;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">/*</span><span class="hljs-comment">             * waitStatus must be 0 or PROPAGATE.  Indicate that we</span><span class="hljs-comment">             * need a signal, but don&#x27;t park yet.  Caller will need to</span><span class="hljs-comment">             * retry to make sure it cannot acquire before parking.</span><span class="hljs-comment">             */</span>            compareAndSetWaitStatus(pred, ws, Node.SIGNAL);        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;    &#125;</code></pre><p>parkAndCheckInterrupt方法，那就是block核心了，阻塞当前线程，并且唤醒是检查是否被中断</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">parkAndCheckInterrupt</span><span class="hljs-params">()</span> </span>&#123;       LockSupport.park(<span class="hljs-keyword">this</span>);       <span class="hljs-keyword">return</span> Thread.interrupted();   &#125;</code></pre><p>综上，公平锁就是通过同步队列来实现多个线程按照申请锁的顺序来获取锁，从而实现公平的特性。非公平锁加锁时不考虑排队等待问题，直接尝试获取锁，所以存在后申请却先获得锁的情况。</p><h2 id="同个线程能不能多次获取同一个锁的角度，分为：可重入锁和不可重入锁"><a href="#同个线程能不能多次获取同一个锁的角度，分为：可重入锁和不可重入锁" class="headerlink" title="同个线程能不能多次获取同一个锁的角度，分为：可重入锁和不可重入锁"></a>同个线程能不能多次获取同一个锁的角度，分为：可重入锁和不可重入锁</h2><p>可重入锁又名递归锁，是指在同一个线程在外层方法获取锁的时候，再进入该线程的内层方法会自动获取锁（前提锁对象得是同一个对象或者class），不会因为之前已经获取过还没释放而阻塞。</p><h3 id="可重入锁："><a href="#可重入锁：" class="headerlink" title="可重入锁："></a>可重入锁：</h3><pre><code>Java中ReentrantLock和synchronized都是可重入锁，可重入锁的一个优点是可一定程度避免死锁。</code></pre><h3 id="不可重入锁："><a href="#不可重入锁：" class="headerlink" title="不可重入锁："></a>不可重入锁：</h3><pre><code>jboss实现的NonReentrantLock是不可重入锁。</code></pre><p>ReentrantLock和NonReentrantLock都继承父类AQS，其父类AQS中维护了一个同步状态status来计数重入次数，status初始值为0。<br>可重入锁的逻辑，在公平锁和非公平锁都有同样逻辑那就是：</p><pre><code class="hljs java"><span class="hljs-comment">//加锁</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryAcquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> acquires)</span> </span>&#123;    <span class="hljs-keyword">final</span> Thread current = Thread.currentThread();    <span class="hljs-keyword">int</span> c = getState();    <span class="hljs-keyword">if</span> (c == <span class="hljs-number">0</span>) &#123;        <span class="hljs-keyword">if</span> (!hasQueuedPredecessors() &amp;&amp;            compareAndSetState(<span class="hljs-number">0</span>, acquires)) &#123;            setExclusiveOwnerThread(current);            <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        &#125;    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (current == getExclusiveOwnerThread()) &#123;        <span class="hljs-keyword">int</span> nextc = c + acquires;                       <span class="hljs-comment">//计数器+1</span>        <span class="hljs-keyword">if</span> (nextc &lt; <span class="hljs-number">0</span>)            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(<span class="hljs-string">&quot;Maximum lock count exceeded&quot;</span>);        setState(nextc);        <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;&#125;<span class="hljs-comment">//解锁 java.util.concurrent.locks.ReentrantLock.Sync#tryRelease</span> <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryRelease</span><span class="hljs-params">(<span class="hljs-keyword">int</span> releases)</span> </span>&#123;    <span class="hljs-keyword">int</span> c = getState() - releases;    <span class="hljs-keyword">if</span> (Thread.currentThread() != getExclusiveOwnerThread())        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalMonitorStateException();    <span class="hljs-keyword">boolean</span> free = <span class="hljs-keyword">false</span>;    <span class="hljs-keyword">if</span> (c == <span class="hljs-number">0</span>) &#123;        free = <span class="hljs-keyword">true</span>;        setExclusiveOwnerThread(<span class="hljs-keyword">null</span>);    &#125;    setState(c);    <span class="hljs-keyword">return</span> free;&#125;</code></pre><p>当线程尝试获取锁时，可重入锁先尝试获取并更新status值，如果status == 0表示没有其他线程在执行同步代码，则把status置为1，当前线程开始执行。如果status != 0，则判断当前线程是否是获取到这个锁的线程，如果是的话执行status+1，且当前线程可以再次获取锁。而非可重入锁是直接去获取并尝试更新当前status的值，如果status != 0的话会导致其获取锁失败，当前线程阻塞。</p><p>释放锁时，可重入锁同样先获取当前status的值，在当前线程是持有锁的线程的前提下。如果status-1 == 0，则表示当前线程所有重复获取锁的操作都已经执行完毕，然后该线程才会真正释放锁。而非可重入锁则是在确定当前线程是持有锁的线程之后，直接将status置为0，将锁释放。</p><p>不可重入锁的逻辑：<br>NonReentrantLock不分公平锁还是非公平锁，state也只有0和1状态，所以无法做到重入。</p><pre><code class="hljs java"><span class="hljs-comment">//org.jboss.netty.util.internal.NonReentrantLock</span>   <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryAcquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> acquires)</span> </span>&#123;        <span class="hljs-keyword">if</span> (compareAndSetState(<span class="hljs-number">0</span>, <span class="hljs-number">1</span>)) &#123;            owner = Thread.currentThread();            <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryRelease</span><span class="hljs-params">(<span class="hljs-keyword">int</span> releases)</span> </span>&#123;        <span class="hljs-keyword">if</span> (Thread.currentThread() != owner) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalMonitorStateException();        &#125;        owner = <span class="hljs-keyword">null</span>;        setState(<span class="hljs-number">0</span>);        <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;    &#125;</code></pre><h2 id="多个线程能不能共享获取同一个锁的角度，分为：共享锁和排他锁"><a href="#多个线程能不能共享获取同一个锁的角度，分为：共享锁和排他锁" class="headerlink" title="多个线程能不能共享获取同一个锁的角度，分为：共享锁和排他锁"></a>多个线程能不能共享获取同一个锁的角度，分为：共享锁和排他锁</h2><p>共享锁，有时也称为读锁，是指该锁可被多个线程所持有。如果线程T对数据A加上共享锁后，则其他线程只能对A再加共享锁，不能加排它锁。获得共享锁的线程只能读数据，不能修改数据。<br>排他锁，有时也称独享锁，有时也称为写锁。是指该锁一次只能被一个线程所持有。如果线程T对数据A加上排它锁后，则其他线程不能再对A加任何类型的锁。获得排它锁的线程即能读数据又能修改数据。JDK中的synchronized和JUC中Lock的实现类就是互斥锁。</p><p>独享锁与共享锁也是通过AQS来实现的，通过实现不同的方法，来实现独享或者共享。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ReentrantReadWriteLock</span></span><span class="hljs-class">        <span class="hljs-keyword">implements</span> <span class="hljs-title">ReadWriteLock</span>, <span class="hljs-title">java</span>.<span class="hljs-title">io</span>.<span class="hljs-title">Serializable</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = -<span class="hljs-number">6992448646407690164L</span>;    <span class="hljs-comment">/** Inner class providing readlock */</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> ReentrantReadWriteLock.ReadLock readerLock;    <span class="hljs-comment">/** Inner class providing writelock */</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> ReentrantReadWriteLock.WriteLock writerLock;    <span class="hljs-comment">/** Performs all synchronization mechanics */</span>    <span class="hljs-keyword">final</span> Sync sync;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * Creates a new &#123;<span class="hljs-doctag">@code</span> ReentrantReadWriteLock&#125; with</span><span class="hljs-comment">     * default (nonfair) ordering properties.</span><span class="hljs-comment">     */</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ReentrantReadWriteLock</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">this</span>(<span class="hljs-keyword">false</span>);    &#125;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * Creates a new &#123;<span class="hljs-doctag">@code</span> ReentrantReadWriteLock&#125; with</span><span class="hljs-comment">     * the given fairness policy.</span><span class="hljs-comment">     *</span><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> fair &#123;<span class="hljs-doctag">@code</span> true&#125; if this lock should use a fair ordering policy</span><span class="hljs-comment">     */</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ReentrantReadWriteLock</span><span class="hljs-params">(<span class="hljs-keyword">boolean</span> fair)</span> </span>&#123;        sync = fair ? <span class="hljs-keyword">new</span> FairSync() : <span class="hljs-keyword">new</span> NonfairSync();        readerLock = <span class="hljs-keyword">new</span> ReadLock(<span class="hljs-keyword">this</span>);        writerLock = <span class="hljs-keyword">new</span> WriteLock(<span class="hljs-keyword">this</span>);    &#125;&#125;<span class="hljs-comment">//java.util.concurrent.locks.ReentrantReadWriteLock.ReadLock</span><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ReadLock</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Lock</span>, <span class="hljs-title">java</span>.<span class="hljs-title">io</span>.<span class="hljs-title">Serializable</span> </span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = -<span class="hljs-number">5992448646407690164L</span>;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Sync sync;        <span class="hljs-comment">/**</span><span class="hljs-comment">         * Constructor for use by subclasses</span><span class="hljs-comment">         *</span><span class="hljs-comment">         * <span class="hljs-doctag">@param</span> lock the outer lock object</span><span class="hljs-comment">         * <span class="hljs-doctag">@throws</span> NullPointerException if the lock is null</span><span class="hljs-comment">         */</span>        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-title">ReadLock</span><span class="hljs-params">(ReentrantReadWriteLock lock)</span> </span>&#123;            sync = lock.sync;        &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">lock</span><span class="hljs-params">()</span> </span>&#123;            sync.acquireShared(<span class="hljs-number">1</span>);        &#125;&#125;<span class="hljs-comment">//java.util.concurrent.locks.ReentrantReadWriteLock.WriteLock</span> <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">WriteLock</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Lock</span>, <span class="hljs-title">java</span>.<span class="hljs-title">io</span>.<span class="hljs-title">Serializable</span> </span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = -<span class="hljs-number">4992448646407690164L</span>;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Sync sync;        <span class="hljs-comment">/**</span><span class="hljs-comment">         * Constructor for use by subclasses</span><span class="hljs-comment">         *</span><span class="hljs-comment">         * <span class="hljs-doctag">@param</span> lock the outer lock object</span><span class="hljs-comment">         * <span class="hljs-doctag">@throws</span> NullPointerException if the lock is null</span><span class="hljs-comment">         */</span>        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-title">WriteLock</span><span class="hljs-params">(ReentrantReadWriteLock lock)</span> </span>&#123;            sync = lock.sync;        &#125;        <span class="hljs-comment">/**</span><span class="hljs-comment">         * Acquires the write lock.</span><span class="hljs-comment">         *</span><span class="hljs-comment">         * &lt;p&gt;Acquires the write lock if neither the read nor write lock</span><span class="hljs-comment">         * are held by another thread</span><span class="hljs-comment">         * and returns immediately, setting the write lock hold count to</span><span class="hljs-comment">         * one.</span><span class="hljs-comment">         *</span><span class="hljs-comment">         * &lt;p&gt;If the current thread already holds the write lock then the</span><span class="hljs-comment">         * hold count is incremented by one and the method returns</span><span class="hljs-comment">         * immediately.</span><span class="hljs-comment">         *</span><span class="hljs-comment">         * &lt;p&gt;If the lock is held by another thread then the current</span><span class="hljs-comment">         * thread becomes disabled for thread scheduling purposes and</span><span class="hljs-comment">         * lies dormant until the write lock has been acquired, at which</span><span class="hljs-comment">         * time the write lock hold count is set to one.</span><span class="hljs-comment">         */</span>        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">lock</span><span class="hljs-params">()</span> </span>&#123;            sync.acquire(<span class="hljs-number">1</span>);        &#125; &#125;</code></pre><p>我们看到ReentrantReadWriteLock有两把锁：ReadLock和WriteLock，由词知意，一个读锁一个写锁，合称“读写锁”。再进一步观察可以发现ReadLock和WriteLock是靠内部类Sync实现的锁。Sync是AQS的一个子类，这种结构在CountDownLatch、ReentrantLock、Semaphore里面也都存在。</p><p>在ReentrantReadWriteLock里面，读锁和写锁的锁主体都是Sync，但读锁和写锁的加锁方式不一样。读锁是共享锁，写锁是独享锁。读锁的共享锁可保证并发读非常高效，而读写、写读、写写的过程互斥，因为读锁和写锁是分离的。所以ReentrantReadWriteLock的并发性相比一般的互斥锁有了很大提升。</p><p>那读锁和写锁的具体加锁方式有什么区别呢？在了解源码之前我们需要回顾一下其他知识。 在最开始提及AQS的时候我们也提到了state字段（int类型，32位），该字段用来描述有多少线程获持有锁。</p><p>在独享锁中这个值通常是0或者1（如果是重入锁的话state值就是重入的次数），在共享锁中state就是持有锁的数量。但是在ReentrantReadWriteLock中有读、写两把锁，所以需要在一个整型变量state上分别描述读锁和写锁的数量（或者也可以叫状态）。于是将state变量“按位切割”切分成了两个部分，高16位表示读锁状态（读锁个数），低16位表示写锁状态（写锁个数）。</p><p>看写锁的加锁源码：</p><pre><code class="hljs java"><span class="hljs-comment">//java.util.concurrent.locks.ReentrantReadWriteLock.Sync#tryAcquire</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryAcquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> acquires)</span> </span>&#123;            <span class="hljs-comment">/*</span><span class="hljs-comment">             * Walkthrough:</span><span class="hljs-comment">             * 1. If read count nonzero or write count nonzero</span><span class="hljs-comment">             *    and owner is a different thread, fail.</span><span class="hljs-comment">             * 2. If count would saturate, fail. (This can only</span><span class="hljs-comment">             *    happen if count is already nonzero.)</span><span class="hljs-comment">             * 3. Otherwise, this thread is eligible for lock if</span><span class="hljs-comment">             *    it is either a reentrant acquire or</span><span class="hljs-comment">             *    queue policy allows it. If so, update state</span><span class="hljs-comment">             *    and set owner.</span><span class="hljs-comment">             */</span>            Thread current = Thread.currentThread();            <span class="hljs-keyword">int</span> c = getState();            <span class="hljs-keyword">int</span> w = exclusiveCount(c);            <span class="hljs-keyword">if</span> (c != <span class="hljs-number">0</span>) &#123;                <span class="hljs-comment">// (Note: if c != 0 and w == 0 then shared count != 0)</span>                <span class="hljs-keyword">if</span> (w == <span class="hljs-number">0</span> || current != getExclusiveOwnerThread())                    <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;                <span class="hljs-keyword">if</span> (w + exclusiveCount(acquires) &gt; MAX_COUNT)               <span class="hljs-comment">//写锁的state之所以为什么可以大于0，那是因为重入锁逻辑</span>                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(<span class="hljs-string">&quot;Maximum lock count exceeded&quot;</span>);                <span class="hljs-comment">// Reentrant acquire</span>                setState(c + acquires);                <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;            &#125;            <span class="hljs-keyword">if</span> (writerShouldBlock() ||                !compareAndSetState(c, c + acquires))                <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;            setExclusiveOwnerThread(current);            <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        &#125;</code></pre><p>这段代码首先取到当前锁的个数c，然后再通过c来获取写锁的个数w。因为写锁是低16位，所以取低16位的最大值与当前的c做与运算（ int w = exclusiveCount©; ），高16位和0与运算后是0，剩下的就是低位运算的值，同时也是持有写锁的线程数目。<br>在取到写锁线程的数目后，首先判断是否已经有线程持有了锁。如果已经有线程持有了锁(c!=0)，则查看当前写锁线程的数目，如果写线程数为0（即此时存在读锁）或者持有锁的线程不是当前线程就返回失败（涉及到公平锁和非公平锁的实现）。<br>如果写入锁的数量大于最大数（65535，2的16次方-1）就抛出一个Error。<br>如果当且写线程数为0（那么读线程也应该为0，因为上面已经处理c!=0的情况），并且当前线程需要阻塞那么就返回失败；如果通过CAS增加写线程数失败也返回失败。<br>如果c=0,w=0或者c&gt;0,w&gt;0（重入），则设置当前线程或锁的拥有者，返回成功！<br>tryAcquire()除了重入条件（当前线程为获取了写锁的线程）之外，增加了一个读锁是否存在的判断。如果存在读锁，则写锁不能被获取，原因在于：必须确保写锁的操作对读锁可见，如果允许读锁在已被获取的情况下对写锁的获取，那么正在运行的其他读线程就无法感知到当前写线程的操作。</p><p>因此，只有等待其他读线程都释放了读锁，写锁才能被当前线程获取，而写锁一旦被获取，则其他读写线程的后续访问均被阻塞。写锁的释放与ReentrantLock的释放过程基本类似，每次释放均减少写状态，当写状态为0时表示写锁已被释放，然后等待的读写线程才能够继续访问读写锁，同时前次写线程的修改对后续的读写线程可见。</p><p>接着是读锁的代码：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> <span class="hljs-title">tryAcquireShared</span><span class="hljs-params">(<span class="hljs-keyword">int</span> unused)</span> </span>&#123;           <span class="hljs-comment">/*</span><span class="hljs-comment">            * Walkthrough:</span><span class="hljs-comment">            * 1. If write lock held by another thread, fail.</span><span class="hljs-comment">            * 2. Otherwise, this thread is eligible for</span><span class="hljs-comment">            *    lock wrt state, so ask if it should block</span><span class="hljs-comment">            *    because of queue policy. If not, try</span><span class="hljs-comment">            *    to grant by CASing state and updating count.</span><span class="hljs-comment">            *    Note that step does not check for reentrant</span><span class="hljs-comment">            *    acquires, which is postponed to full version</span><span class="hljs-comment">            *    to avoid having to check hold count in</span><span class="hljs-comment">            *    the more typical non-reentrant case.</span><span class="hljs-comment">            * 3. If step 2 fails either because thread</span><span class="hljs-comment">            *    apparently not eligible or CAS fails or count</span><span class="hljs-comment">            *    saturated, chain to version with full retry loop.</span><span class="hljs-comment">            */</span>           Thread current = Thread.currentThread();           <span class="hljs-keyword">int</span> c = getState();           <span class="hljs-keyword">if</span> (exclusiveCount(c) != <span class="hljs-number">0</span> &amp;&amp;               getExclusiveOwnerThread() != current)               <span class="hljs-keyword">return</span> -<span class="hljs-number">1</span>;          <span class="hljs-comment">// 如果其他线程已经获取了写锁，则当前线程获取读锁失败，进入等待状态</span>           <span class="hljs-keyword">int</span> r = sharedCount(c);           <span class="hljs-keyword">if</span> (!readerShouldBlock() &amp;&amp;               r &lt; MAX_COUNT &amp;&amp;               compareAndSetState(c, c + SHARED_UNIT)) &#123;               <span class="hljs-keyword">if</span> (r == <span class="hljs-number">0</span>) &#123;                   firstReader = current;                   firstReaderHoldCount = <span class="hljs-number">1</span>;               &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (firstReader == current) &#123;                   firstReaderHoldCount++;               &#125; <span class="hljs-keyword">else</span> &#123;                   HoldCounter rh = cachedHoldCounter;                   <span class="hljs-keyword">if</span> (rh == <span class="hljs-keyword">null</span> || rh.tid != getThreadId(current))                       cachedHoldCounter = rh = readHolds.get();                   <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (rh.count == <span class="hljs-number">0</span>)                       readHolds.set(rh);                   rh.count++;               &#125;               <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;           &#125;           <span class="hljs-keyword">return</span> fullTryAcquireShared(current);       &#125;</code></pre><p>可以看到在tryAcquireShared(int unused)方法中，如果其他线程已经获取了写锁，则当前线程获取读锁失败，进入等待状态。如果当前线程获取了写锁或者写锁未被获取，则当前线程（线程安全，依靠CAS保证）增加读状态，成功获取读锁。读锁的每次释放（线程安全的，可能有多个读线程同时释放读锁）均减少读状态，减少的值是“1&lt;&lt;16”。所以读写锁才能实现读读的过程共享，而读写、写读、写写的过程互斥。</p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
